apiVersion: apps/v1
kind: Deployment
metadata:
  name: agc-utils
  namespace: agc
  labels:
    app: agc-utils
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agc-utils
  template:
    metadata:
      labels:
        app: agc-utils
    spec:
      initContainers:
        - name: init-image-dirs
          image: alpine:3.21
          command: ["mkdir", "-p", "/images/flag_images", "/images/warn_images"]
          volumeMounts:
            - name: images
              mountPath: /images

      containers:
        - name: agc-utils
          image: ghcr.io/anime-gaming-cafe/agc-utils
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 8080
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
          volumeMounts:
            - name: config
              mountPath: /app/config.ini
              subPath: config.ini
            - name: transcripts
              mountPath: /app/data/tickets/transcripts
            - name: images
              mountPath: /app/flag_images
              subPath: flag_images
            - name: images
              mountPath: /app/warn_images
              subPath: warn_images
          resources:
            requests:
              cpu:    250m
              memory: 512Mi
            limits:
              cpu:    1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10

        - name: apache-transcripts
          image: httpd:2.4-alpine
          ports:
            - name: transcripts
              containerPort: 80
          volumeMounts:
            - name: transcripts
              mountPath: /usr/local/apache2/htdocs/transcripts
              readOnly: true
            - name: apache-config
              mountPath: /usr/local/apache2/conf/httpd.conf
              subPath: httpd.conf
          resources:
            requests:
              cpu:    50m
              memory: 64Mi
            limits:
              cpu:    200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /transcripts/
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /transcripts/
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10

        - name: apache-images
          image: httpd:2.4-alpine
          ports:
            - name: images
              containerPort: 81
          volumeMounts:
            - name: images
              mountPath: /usr/local/apache2/htdocs
              readOnly: true
            - name: apache-config
              mountPath: /usr/local/apache2/conf/httpd.conf
              subPath: httpd-images.conf
          resources:
            requests:
              cpu:    50m
              memory: 64Mi
            limits:
              cpu:    200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 81
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 81
            initialDelaySeconds: 5
            periodSeconds: 10

      volumes:
        - name: config
          secret:
            secretName: agc-utils-config

        - name: transcripts
          persistentVolumeClaim:
            claimName: agc-transcripts

        - name: images
          persistentVolumeClaim:
            claimName: agc-images

        - name: apache-config
          configMap:
            name: agc-utils-apache
